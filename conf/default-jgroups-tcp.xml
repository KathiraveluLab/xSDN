<config xmlns="urn:org:jgroups"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="urn:org:jgroups http://www.jgroups.org/schema/jgroups.xsd">
   <TCP
        bind_addr="${jgroups.tcp.address:127.0.0.1}"
        bind_port="${jgroups.tcp.port:7800}"
        port_range="30"
        recv_buf_size="20m"
        send_buf_size="640k"

        thread_pool.min_threads="2"
        thread_pool.max_threads="30"
        thread_pool.keep_alive_time="60000"
        thread_pool.rejection_policy="Discard"
        />

   <!-- Ergonomics, new in JGroups 2.11, are disabled by default in TCPPING until JGRP-1253 is resolved -->
   <!--
   <TCPPING timeout="3000"
            initial_hosts="localhost[7800],localhost[7801]"
            port_range="5"
            num_initial_members="3"
            ergonomics="false"
        />
   -->

   <MPING bind_addr="${jgroups.bind_addr:127.0.0.1}" break_on_coord_rsp="true"
      mcast_addr="${jgroups.mping.mcast_addr:228.2.4.6}"
      mcast_port="${jgroups.mping.mcast_port:43366}"
      ip_ttl="${jgroups.udp.ip_ttl:2}"/>
   <MERGE3/>

   <FD_SOCK/>
   <FD_ALL3 timeout="15s" interval="3s"/>
   <VERIFY_SUSPECT2 timeout="1.5s"/>

   <pbcast.NAKACK2 use_mcast_xmit="false"
                   xmit_interval="1s"
                   xmit_table_num_rows="100"
                   xmit_table_msgs_per_row="10000"
                   xmit_table_max_compaction_time="10s"/>
   <UNICAST3 xmit_interval="500ms"
             xmit_table_num_rows="20"
             xmit_table_msgs_per_row="10000"
             xmit_table_max_compaction_time="10s"
             conn_expiry_timeout="0"/>

   <pbcast.STABLE stability_delay="500ms" desired_avg_gossip="5s" max_bytes="1M"/>
   <pbcast.GMS print_local_addr="false" join_timeout="3s"/>

   <MFC max_credits="2M" min_threshold="0.40"/>
   <FRAG2 frag_size="30k"/>
   <RSVP timeout="60s" resend_interval="500ms" ack_on_delivery="false" />
</config>
